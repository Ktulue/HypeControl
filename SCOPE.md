# Scope Contract
**Task:** Fix resub callout false-trigger (#44) | **Plan:** docs/superpowers/plans/2026-04-24-resub-callout-false-trigger.md | **Date:** 2026-04-24 | **Status:** ACTIVE

## In Scope

### Files
- `src/content/detector.ts` — add `CHAT_CALLOUT_SEED_DATATARGETS`, `CHAT_CALLOUT_SUFFIX_RE`, `isInsideChatCallout()` helper, wire early-return into `isPurchaseButton()`
- `tests/content/detector.test.ts` — **NEW.** 17 Jest tests (5 callout-exclusion + 12 baseline via `test.each`)
- `package.json` — add `jest-environment-jsdom` devDep; patch bump 1.0.9 → 1.0.10
- `package-lock.json` — regenerated by `npm install`
- `manifest.json` — patch bump 1.0.9 → 1.0.10
- `CLAUDE.md` — consolidate Version Management + Versioning sections into one explicit rule naming all three manifests
- `docs/dev/HypeControl-TODO.md` — resolve pre-existing merge conflict (lines 322-326); update header date/version; add v1.0.9 chat-command-interception section (missing); add v1.0.10 resub-callout-fix section; add deferred Option C entry

### Features / Criteria
- Chat-callout ancestor exclusion: seed list + anchored `/-callout(__|$)/` suffix regex
- Early-return placed after `elementInfo` construction, before `IGNORE_LABELS` check
- Debug log on ignored clicks
- TDD: red commit → green commit cadence
- `npm test` all-green before commit of any test task
- `npm run build` attempted once at end; no retry on failure
- Open PR, do NOT merge

### Explicit Boundaries
- Version bump: Chrome `manifest.json` + `package.json` ONLY. `manifest.firefox.json` held at 1.0.2.
- `CLAUDE.md` — only the two versioning sections are touched. `## Build` section duplication is left alone.
- No changes to `interceptor.ts`, UI, settings, storage, migrations.

## Out of Scope (all four captured in plan Task 7 Step 4 → HypeControl-TODO.md Deferred section)
- **Detector allowlist rewrite (Option C).** Deferred.
- **Require `BUTTON` tag for label-keyword path.** Deferred per spec Follow-ups.
- **`manifest.firefox.json` version bump.** Reserved for upcoming 1.1.0 release cut (Chrome + Firefox lockstep catch-up).
- **Removing duplicate `## Build` section from CLAUDE.md.** Low-priority maint, harmless duplication.

## Not Captured as Follow-up (by design)
- **PR merge.** User approves separately per global workflow rule — not a deferred task.
- **Any refactor of existing detector logic** beyond inserting the new early-return. Not a planned follow-up.

# Scope Change Log
| # | Category | What | Why | Decision | Outcome |
|---|----------|------|-----|----------|---------|

# Follow-up Tasks
(none yet)
